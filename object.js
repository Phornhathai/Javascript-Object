// ประกาศตัวแปรใหม่ที่มีชื่อว่า product. โดยที่ตัวแปรดังกล่าวควรจะเป็น object และมี key และ value อย่างเช่นตัวอย่างด้านล่างนี้

// name - กำหนดให้เป็น string "Gummy Bears"
// inStock -กำหนดให้เป็น boolean true
// price  - กำหนดให้เป็น number 1.99
// flavors -กำหนดให้เป็น array ที่มีค่าเป็น strings อย่างน้อย 3 ค่า เช่น ["grape", "apple", "cherry"]

let product = {
    name : "Gummy Bears",
    inStock : true,
    price : 1.99,
    flavors : ["grape", "apple", "cherry"]
}

console.log(product);